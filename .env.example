# The committed official snapshot is loaded from the Vite base by default
# (/packages.json on a root-hosted deployment).
# Optionally override it with another compatible same-origin path or HTTP(S) endpoint.
# VITE_PACKAGE_API_URL=https://example.com/api/v1/packages
